import React from 'react';
import Nav from '../Nav/Nav';
import Player from '../Player/Player';

const HeaderPlayer = () => {
  return (
    <header className="header header-player">
      <Nav />
      <Player />
    </header>
  );
};

export default HeaderPlayer;
